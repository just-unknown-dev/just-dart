name: just_dart
description: Shared Dart utilities for Just Ecosystem.
version: 0.1.0
homepage: https://github.com/just-unknown-dev/just-dart
repository: https://github.com/just-unknown-dev/just-dart
issue_tracker: https://github.com/just-unknown-dev/just-dart/issues

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

flutter:
